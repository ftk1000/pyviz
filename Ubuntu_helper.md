### how to install gcc (or other packages)

    sudo apt update
    sudo apt install gcc --fix-missing
    
    
    sudo apt clean
    sudo apt autoclean
    sudo apt update
    sudo apt update --no-cache
    
    
    
